#!/usr/bin/node

const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const line of lines) console.log(line);

// This code iterates through an array of strings and prints each string to the console.